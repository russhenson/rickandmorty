import { CharacterList } from "../components/index";

export const Landing = () => {
  return (
    <div style={{position: 'relative', padding: '6rem 2rem 2rem 2rem'}}>
      <CharacterList />
    </div>
  )
}